import 'package:flutter/material.dart';

class ServerErrorDialog extends StatelessWidget {
  const ServerErrorDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
